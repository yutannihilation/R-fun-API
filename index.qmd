---
# TODO: can I pass the result of R.version$`svn rev` to title?
---

# `tools:::funAPI()` (rev: `{r} R.version[["svn rev"]]`)

```{r}
#| echo: false

reactable::reactable(
  tools:::funAPI(),
  filterable = TRUE,
  defaultPageSize = 30
)
```
